/// <reference types="node" />
export declare class ProofOfOwnership {
    didProver: string;
    didVerifier: string;
    publicKey: Buffer;
    signature?: string;
    timestamp?: Date;
    constructor(didProver: string, didVerifier: string, publicKey: Buffer, timestamp?: Date, signature?: string);
}
