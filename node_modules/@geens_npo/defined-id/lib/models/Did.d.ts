import { IDid } from "..";
export declare class Did implements IDid {
    method: string;
    nemPublicKey: string;
    salt: string;
    constructor(method: string, nemPublicKey: string, salt?: string);
    format(): string;
    private generateIdentifier;
}
