import nem2Sdk = require("symbol-sdk");
import NetworkType = nem2Sdk.NetworkType;
export declare class Blockchain {
    static getNetworkTypeByBlockchain(blockchain: string): NetworkType | undefined;
}
