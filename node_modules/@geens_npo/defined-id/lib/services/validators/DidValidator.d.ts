import { IDid } from "../..";
export declare class DidValidator {
    private static methodRegex;
    private static didStringRegex;
    private static saltRegex;
    private static hexRegex;
    static isMethodValid(method: string): boolean;
    static isSaltValid(salt: string): boolean;
    static isNemPublicKeyValid(nemPublicKey: string): boolean;
    static isDidStringValid(didString: string): boolean;
    static validateDidModel(did: IDid): string[];
}
