import { IKey } from "../..";
export declare class KeyValidator {
    private static derivePathRegex;
    static isDerivePathValid(derivePath: string): boolean;
    static isNaturalNumber(number: number): boolean;
    static isEncodeValid(encode: string): boolean;
    static validateKeyModel(key: IKey): string[];
}
