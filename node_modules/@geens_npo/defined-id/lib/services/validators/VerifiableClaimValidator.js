"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.VerifiableClaimValidator = void 0;
class VerifiableClaimValidator {
    static isIdValid(id) {
        return this.idRegex.test(id);
    }
    static isDateValid(date) {
        return !!Date.parse(date);
    }
    static validateVerifiableClaim(verifiableClaim) {
        const errors = [];
        if (!verifiableClaim.id)
            errors.push('Claim: Id is required');
        else if (!this.isIdValid(verifiableClaim.id))
            errors.push('Claim: Id is invalid');
        if (Object.keys(verifiableClaim).length < 2)
            errors.push('Claim: At least 1 property is required');
        return errors;
    }
}
exports.VerifiableClaimValidator = VerifiableClaimValidator;
VerifiableClaimValidator.idRegex = new RegExp("^http://defined.id/vc.*$");
