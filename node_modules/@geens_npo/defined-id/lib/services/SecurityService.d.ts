/// <reference types="node" />
export declare class SecurityService {
    static scrypt(password: string): string;
    static sha256Hash(bytes: Uint8Array): string;
    static derivePath(path: string, hexSeed: string): {
        key: Buffer;
        chainCode: Buffer;
    };
    static base58Encode(buffer: Buffer): string;
    static pemEncode(buffer: Buffer): string;
    static base58CheckEncode(text: string): string;
    static base58CheckDecode(text: string): string;
    static ripemd160Hash(text: string): string;
    static sha256StringHash(text: string): string;
}
