import nem2Sdk = require("symbol-sdk");
import { NemAccount } from "..";
import PublicAccount = nem2Sdk.PublicAccount;
import Transaction = nem2Sdk.Transaction;
export declare class NemTransactionService {
    static createTimestampTransaction(nemAccount: NemAccount, hash: string, nodeUri: string): Promise<nem2Sdk.SignedTransaction>;
    static announceTransaction(signedTransaction: any, nodeUri: string): Promise<string>;
    static timestampTransaction(nemAccount: NemAccount, hash: string, nodeUri: string): Promise<string>;
    static getTransactionIndexForHash(pubKey: string, hash: string, nodeUri: string, blockchain: string): Promise<unknown>;
    static getTransactionIndexForHashAndAccount(account: PublicAccount, hash: string, nodeUri: string): Promise<unknown>;
    static getRegisteredHashes(account: PublicAccount, nodeUri: string): Promise<string[]>;
    static getRegisteredHashesFromTxList(txs: Transaction[]): string[];
    static getTransactionIndexForHashAndTxList(hash: string, txs: Transaction[]): number;
}
