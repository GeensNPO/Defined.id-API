import nem2Sdk = require("symbol-sdk");
import PublicAccount = nem2Sdk.PublicAccount;
export declare class IdentityService {
    static AccountRegistered(account: PublicAccount, nodeUri: string): Promise<boolean>;
}
