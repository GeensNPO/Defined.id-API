import nem2Sdk = require("symbol-sdk");
import Transaction = nem2Sdk.Transaction;
export declare class TransactionFilter {
    static transferTransactionSameSenderReceiver(tx: Transaction): boolean;
    static sortTransactionsByHeight(a: Transaction, b: Transaction): number;
}
