import { NamespaceId } from '../namespace/NamespaceId';
import { UInt64 } from '../UInt64';
import { Mosaic } from './Mosaic';
/**
 * NetworkHarvestLocal mosaic
 *
 * This represents the per-network harvest mosaic. This mosaicId is aliased
 * with namespace name `cat.harvest`.
 *
 * @since 0.10.2
 */
export declare class NetworkHarvestLocal extends Mosaic {
    /**
     * namespaceId of `currency` namespace.
     *
     * @type {Id}
     */
    static NAMESPACE_ID: NamespaceId;
    /**
     * Divisiblity
     * @type {number}
     */
    static DIVISIBILITY: number;
    /**
     * Initial supply
     * @type {number}
     */
    static INITIAL_SUPPLY: number;
    /**
     * Is tranferable
     * @type {boolean}
     */
    static TRANSFERABLE: boolean;
    /**
     * Is Supply mutable
     * @type {boolean}
     */
    static SUPPLY_MUTABLE: boolean;
    /**
     * constructor
     * @param owner
     * @param amount
     */
    private constructor();
    /**
     * Create NetworkHarvestLocal with using NetworkHarvestLocal as unit.
     *
     * @param amount
     * @returns {NetworkHarvestLocal}
     */
    static createRelative(amount: UInt64 | number): NetworkHarvestLocal;
    /**
     * Create NetworkHarvestLocal with using micro NetworkHarvestLocal as unit,
     * 1 NetworkHarvestLocal = 1000000 micro NetworkHarvestLocal.
     *
     * @param amount
     * @returns {NetworkHarvestLocal}
     */
    static createAbsolute(amount: UInt64 | number): NetworkHarvestLocal;
}
